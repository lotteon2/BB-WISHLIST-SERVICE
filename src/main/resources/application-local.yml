server:
  port: 8500

spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://localhost:3306/bb-wishlist-service?serverTimezone=Asia/Seoul
    username: root
    password: k1651227
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true


service:
  product:
    domain: http://localhost:5000
  store:
    domain: http://localhost:4000
